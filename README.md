# Projet_mifa
this projet have been initiated by us in 2026. the plan was to create a wed site with html, css ,js maybe python. i don't really know the goal but i'm in .

1- create the page htlm,css and js to start



/project-root
│
├── index.html          # Point d'entrée unique
├── /assets             # Images, sons, icônes
├── /css
│   ├── main.css        # Styles globaux (reset, variables)
│   └── components.css  # Styles des composants (boutons, cartes)
│
└── /js
    ├── app.js          # Initialisation et routage simple
    ├── data.js         # Vos contenus (JSON/Objets)
    ├── store.js        # Gestion de l'état (score, progression)
    ├── /modules
    │   ├── ui.js       # Fonctions qui manipulent le DOM
    │   ├── engine.js   # Logique de calcul (vérification des réponses)
    │   └── storage.js  # Interaction avec le LocalStorage